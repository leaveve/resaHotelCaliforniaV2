<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<title>Modifier la chambre</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light d-flex justify-content-center align-items-center vh-100">

<div class="card p-4 shadow" style="width:400px;">
    <h2 class="text-center mb-4">Modifier la chambre</h2>

    <form action="/chambres/<%= chambre.id %>/edit" method="POST">

        <div class="mb-3">
            <label class="form-label">Numéro</label>
            <input type="text" name="numero" value="<%= chambre.numero %>" class="form-control" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Type</label>
            <input type="text" name="type" value="<%= chambre.type %>" class="form-control" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Prix (€)</label>
            <input type="number" name="prix" value="<%= chambre.prix %>" class="form-control" required>
        </div>

        <button class="btn btn-primary w-100">Sauvegarder</button>
    </form>

    <a href="/chambres" class="btn btn-secondary w-100 mt-3">Retour</a>
</div>

</body>
</html>
